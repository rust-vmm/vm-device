[package]
name = "vm-device"
version = "0.1.0"
authors = ["Samuel Ortiz <sameo@linux.intel.com>"]
repository = "https://github.com/rust-vmm/vm-device"
license = "Apache-2.0"

[dependencies]
vmm-sys-util = "~0"

[dev-dependencies]
matches = ">=0"
